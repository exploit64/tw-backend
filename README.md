# TestWatch Backend

Серверная часть системы агрегации и хранения результатов тестов.

## Быстрый запуск

Готовый Docker-образ доступен на Docker Hub:

```bash
docker run -d dpawson/tw-backend:latest
